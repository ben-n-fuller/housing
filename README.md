# Housing

Notebook for estimating net worth change across rent vs. buy strategies over different time horizons using Monte Carlo simulation.

## Setup

```
uv venv && uv pip install -e .
```

Then open `housing_calculations.ipynb` with the kernel pointed at `.venv/bin/python`.
